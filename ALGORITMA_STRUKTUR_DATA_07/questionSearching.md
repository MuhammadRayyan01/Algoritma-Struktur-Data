1.What is the difference between the methods displayData and displayPosition in StudentSearch class?
answer:
in display data, the function is to show al the data from 0 to idx, while the display position is to show the index that is the element we search

2.What is the function of break in this following program code?
answer:
if the search is found out, the index wil be noted at ( position = i), and then we need to break the loop so the position will not change its value, even though actually the if statement is not fulfilled so the position will not change the condition.

3.If the NIM data inserted is not sorted from smallest to biggest value, will the program encounter an error? Is the result still correct? Why is that?
answer: result still correct, because the search function is suiting the search with the data, doesnt care about the queue

4.Look at findSeqSearch method, why position is initialized by -1 instead of 0?
answer: because 0 is an valid index, -1 is not.
