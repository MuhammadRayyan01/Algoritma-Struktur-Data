question 3.2.3

1. yes, a class that create an array will have the same attribute and method. in OOP concept, this means that all student have the same attribute and function even though each value is not always the same
   2.from the line above, it means that we declare an array inside our class (in mine it was "Student19"), with the size of array is 3
   3.to declare the first row of the array, and so on
   4.to input the data in the first row of array
   5.it is a standard setting from modern programming language where we can import data from one file to another, and the reason why we do that in OOP because it support programmer to get understand more easily, more legible.

question 3.3.3
1.Suppose you have a new array array of type Students named myArrayOfStudents . Why does the following code cause an error?
answer: because we still havent instantiate the array so it must be like this first:
"ArrayofMahasiswa[i] = new Student19();"
